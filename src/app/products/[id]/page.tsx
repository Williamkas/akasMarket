export default function ProductPage() {

  // Este será un componente renderizado del lado del servidor por la tanto usaremos la función getProductDetails del servicio productService.ts 
  // Ubicada en src/services/productService.ts
  return (
    <div>
      <h1>Product Id</h1>
      {/* Aquí puedes incluir más contenido o componentes */}
    </div>
  );
}
